import Quickshell
import QtQuick
import qs.common

PanelWindow {
    color: Theme.colors.background
    anchors {
        top: true
        left: true
        bottom: true
    }

    implicitWidth: Theme.barSize
}
