###################
### MY PROGRAMS ###
###################
$imagesFolder = xdg-user-dir PICTURES

$terminal = kitty
$fileManager = nautilus -w
$menu = $HOME/.config/rofi/launchers/type-3/launcher.sh
$web = zen-browser
$web-open = $web
$screenlock = hyprlock
$colorpicker = hyprpicker -arq
$notion = $web-open "https://www.notion.so/miguedm/Inicio-65fca48212e3468391b8d0b336e38ac2"
$calendar = $web-open "https://calendar.notion.so/"
$llm = $web-open "https://chatgpt.com/"
$github = $web-open "https://github.com/migueel15"
$github = notify-send $imagesFolder
$screenshot = grim -l 0 -g "$(slurp -d)" - | tee "$HOME/Imágenes/$(date +%Y%m%d-%H%M%S).png"| wl-copy